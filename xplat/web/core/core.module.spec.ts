describe('Core module generic test', () => {
  it('Should be true', () => {
    expect(true).toBeTruthy();
  });
});

/*
import { DesktopStaterCoreModule } from './core.module';

describe('DesktopStaterCoreModule', () => {
  it('should work', () => {
    expect(new DesktopStaterCoreModule(null)).toBeDefined();
  });
});
*/
